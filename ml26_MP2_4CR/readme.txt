CS 418 FALL2018
Mengyuan Li
ml26

Running the program:

The html file can directly run in FireFox. It can also work in Chrome, but since
Chrome prevents users to load data from local files, please install node.js and httpserver to make it work in chrome.

